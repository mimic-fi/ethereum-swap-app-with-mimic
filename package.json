{
  "name": "ethereum-swap-app-with-mimic",
  "private": true,
  "version": "1.0.0",
  "description": "Ethereum swap app with Mimic",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/mocha",
      "**/mocha/**"
    ]
  }
}
